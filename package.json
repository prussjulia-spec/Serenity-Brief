{
  "name": "serenity-brief",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build-apps-script.mjs",
    "build:pages": "node build-pages.mjs",
    "check": "node --check app.js && node --check build-apps-script.mjs && node --check build-pages.mjs && node --check functions/api/submit.js && node --check < google-apps-script/Code.gs",
    "test": "npm run check && npm run build && npm run build:pages"
  }
}
